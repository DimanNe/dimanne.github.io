title: AI test

# **AI test**


### **Test 1**

Can you write a function in C++, makeUnique, that infers the type of variable on the left side of assignment,
takes arbitrary number of arguments (template parameter pack) that are needed to initialise the type on the
left.
For example: std::unique_ptr<std::string> a = makeUnique(“asdf”); should infer the type: std::string,
and then create a string from “adsf” and return it wrapped in uniuque_ptr.


### **Test 2**


Kill mouse


### **Test 3**


Circular train


### **Test 4**

There are four cards lying on the table, with the following written on top: "A", "B", "4", "5". What is
written on the opposite sides of the cards is unknown. What is the smallest number of cards that need
to be turned over to verify the truth of the statement: "If an even number is written on one side of
the card, then a vowel letter is on the other"?

### **Test 5**

есть верёвки (так много, как нужно, включая бесконечное количество верёвок), каждая из которых горит 1 час, если её поджечь с одного конца. Но не обязательно равномерно, т.е. за полчаса может сгореть не половина её длины. Как отмерить 55 минут?

There are ropes (as many as needed, including an infinite number of ropes), each of which burns for 1 hour if lit from one end. But not necessarily uniformly, i.e., in half an hour, not half of its length may burn. How to measure 55 minutes?


### **Test 6**

https://grok.com/share/c2hhcmQtMi1jb3B5_96a4693a-f407-4f2e-8da4-ef648d388853
